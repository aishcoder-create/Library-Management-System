ShelfX backend scaffold

- Express + MongoDB (mongoose)
- Auth (JWT + bcrypt)
- Book CRUD with optional cover upload (multer)

Quick start:
1) cd server
2) cp .env.example .env and edit values
3) npm install
4) npm run dev
